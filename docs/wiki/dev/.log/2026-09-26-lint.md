## [2026-09-26] lint | full pass — five findings, three sharpenings

Up: [dev](../dev.md) · previous: [2026-09-24-lint](2026-09-24-lint.md) · ledger:
[lint-calibration](lint-calibration.md).

**§0** ledger read; three entries appended plus one note. **§1 doc-vs-code:** lock-order table
clean (42 lock fields across the workspace, 0 undeclared; both aliases named); watch-channel RMW
sweep clean (one `borrow()`→`send()` hit is a receiver-to-sender forward in `capability_handle.rs`,
not a read-modify-write); wire v12 / `PREV = 11` consistent everywhere live
(`production-readiness.md`'s "wire v10+" is a floor, correct); `swim_failure_detector` default `true`
matches; KV-namespace gate clean (30 prefixes, both lists); CI-gate block matches `ci.yml`'s `run:`
lines; front-door host, features and companion tags correct. **§2 staleness:** every commit since
09-24 has an ingest entry. **§3:** no dead links, no orphans, every folder-note present; the two
dead inbound anchors are in dated `.log` entries only. **§4:** `dev/examples.md` cites the
front-door matrix; every example category in the tree is represented.

| # | Check | Finding | Fix |
|---|---|---|---|
| 1 | §1 named gates (mechanical sweep) | `history.md` cited `a_replayed_write_does_not_touch_the_disk`, renamed in v2.9.1 | citation updated with the rename and why |
| 2 | §1 front door | install pins `v2.15.0`; newest tag `v2.15.1` | pins → `v2.15.1`; `RELEASING.md` §6 gains the pin as an anchor |
| 3 | §1 endpoints | guide 05 diagram: `POST /invoke`, no such route | label → `rpc_call("skill.invoke", …)`, which is what `invoke.sh` does |
| 4 | §4 design citations | `action-envelope-ae2.md` and `legible-emergence-content-plane.md` uncited from the wiki | cited from `dev/dev.md` and `dev/diagnostics.md` |
| 5 | §4 UI contract | `control_envelope_viz`, `coordination_viz` compliant but unclassified (page said 12 hits, tree 14) | classification line re-derived at 14; the pass now diffs the count |

**Sharpenings folded into `.claude/commands/wiki-lint.md`:** the identifier sweep is mechanical
(every backticked snake_case identifier on non-`.log` pages, grepped against the tree); the UI
classification line pins the hit count and the pass diffs it; a stale front-door pin is two findings
(the doc, and the release step that skipped its new §6 anchor).

Pages touched: `dev/history.md` · `dev/dev.md` · `dev/diagnostics.md` · `dev/ui-example-contract.md`
· `docs/guide/building-on-mycelium.md` · `docs/guide/05-skills.md` · `RELEASING.md` ·
`.claude/commands/wiki-lint.md` · this log · the ledger. Nothing found that pointed at a code bug
rather than a page. No new top-level section.
