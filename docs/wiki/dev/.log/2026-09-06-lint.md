# wiki-lint — 2026-09-06 (post plan rev 1.6 / reason 0.6.2)

Run after rev 1.4–1.6 of the contracts-axis plan, `mycelium-reason` 0.6.1/0.6.2 and the directory fsync —
22 merge commits since the 2026-09-05 lint. Findings by check:

## §0 Calibration ledger
Read first. Applied mechanically: the per-occurrence reserved-list diff (both `building-on` forms and the
blockquote's companion paragraph reconciled: 26/26 prefixes each) and the link sweep over every guide and
operations page resolved relative to its own file.

## §1 Doc-vs-code — 4 findings (one a must-work violation; no code bug)
- **Runbook URL that 404s (F1, ledger entry):** `diagnostics.md` and `metrics.md` said `GET /consensus/lock/{name}`;
  the route `/consensus/{slot}` matches one segment and a lock's slot is `lock/{name}` — probe: literal 404,
  `lock%2F{name}` 200 with the slot. Both runbooks now give the encoded form and say why; probe kept as
  `regression_lock_slot_reaches_consensus_slot_route_only_percent_encoded`. The route shape itself (a wildcard
  would accept the literal) is a design question left to the maintainer, not changed here.
- **Front-door tag pin (F2):** `building-on-mycelium.md` pinned `mycelium-reason-v0.6.0`; 0.6.2 is current. Fixed;
  the check now diffs every `tag = "…"` pin against the newest tag.
- **`docs/operations/companions.md` (F3):** "0.6.0 since 2026-09-04" → the 0.6.x line to 0.6.2. Fixed.
- **`CLAUDE.md` Active work (F4):** described reason 0.6.0 and "no code yet; next: item 1 PR 1"; now names 0.6.1/0.6.2,
  plan rev 1.6 (§12, §13, §6.7 RA), Phase 0 + RA0 as the code so far, and the four next steps. Fixed.
- **Clean:** lock-order table — 67 `Mutex<|RwLock<` sites + 2 aliases (`GossipRxs` → row 5 `gossip_rxs`, `SenderLog`)
  reconciled by field, complete. Watch-RMW sweep: one hit, the documented receiver-borrow relay
  (`capability_handle.rs:558`). 11 cited regression/probe gates all exist. `WIRE_VERSION` 12 / `PREV` 11 /
  `MAX_KV_WRITE_BYTES` as stated; no stale wire pins outside historical logs. KV-namespace table: every non-test
  writer prefix has a row (`consistent/`, `leader/`, `lock/` are consensus slots; `reason/` rides `log/`). CI-gate
  block ↔ `ci.yml` `run:` lines: all present (as `ci-retest.sh` forms). `dev/operations.md` endpoints all literal in
  `http.rs` (`/bulk/{id}` is `{corr_id}`). Sub-handle accessor names (8) and feature flags match.

## §2 Staleness — 2 findings
- **`dev/history.md` (F5):** the delivery ledger stopped at rev 1.3 / reason 0.6.1 — added reason 0.6.2, rev 1.4/1.5/1.6
  (RA slice), the directory fsync (#183), analysis Run 61, doc-coverage run 16. Fixed.
- **`wiki.md` front door (F6):** the knowledge-store table called `docs/plans/README.md` "all engineering plans shipped"
  and had no row for the adopted v3.0 plan of record. Row reworded; plan-of-record row added. Fixed.

## §3 Orphans & dead links — clean
81 files (wiki + every guide/operations page + the front doors + the plan), resolved relative to each file: 0 dead,
0 orphans, every folder-note present. The one anchor the sweep flagged (`faq.md#why-not-langgraph--temporal--nats--`)
is correct — GitHub keeps the trailing hyphens from `/ …?`; the sweep's slugger stripped them (script note, not a
finding). Inbound `examples/README.md#…` links: the two non-link prose hits are old log entries.

## §4 Coverage — clean
`docs/design/*.md` all cited from the wiki. `dev/examples.md` cites the README matrix as canonical (tree: 5 crates
with examples + 22 coop bins + 11 top-level + 2 showcases). UI-example contract: 12 `include_str!` hits, all classified.
New durable knowledge since the last pass (rev 1.4–1.6, 0.6.2, RA) has its log entries in
`.log/2026-09-05-v3-contracts-axis.md` and is now in the ledger and on the front door.

## Needs a user decision
- Every `git = "https://github.com/RichardEko/mycelium"` URL (guide install snippet, three SDK READMEs, both decks,
  paper sources, root README) predates the move to `RichardTathata/mycelium`. GitHub redirects, so nothing breaks
  today; rewriting them is a repo-wide choice (the paper-submission pins may be deliberate).
- `/consensus/{slot}` as a wildcard route would make the runbooks' natural URL work without encoding; a small change,
  but a routing-surface decision (the `required_scope` table keys on the route pattern).
