# wiki-lint — 2026-09-04 (post PAIR-imports + Run 60)

Run the same day as the reason 0.6.0 / core auth-fix arc and analysis Run 60 (~25 commits since the
2026-08-16 lint). Findings by check:

## §1 Doc-vs-code — 5 findings (2 CODE-side, 3 doc-side)
- **CI-gate list vs workflow (the 2026-07-11 ledger pattern, again):** `testing.md` lists
  `cargo clippy --lib --tests --features compliance` in "the full CI gate (for reference)"; `make
  check` has run it since 2026-07-22 but `.github/workflows/ci.yml` never did — compliance-gated code
  (WS1–WS5) was lint-covered locally only. **Fixed in CI** (one `run:` line in the Clippy job), not by
  softening the page.
- **KV-namespace table (third miss in this area):** SkillRunner's plain audit trail writes
  `audit/{ts_unix_nanos}/{node}` (`src/bin/skillrunner/audit.rs`, the `not(compliance)` path) — a
  top-level prefix absent from `src/lib.rs` and from `building-on`'s reserved list. Row added; the
  guide's *bullet* reserved list also lacked `facts/` and `manifest/` that the 2026-07-20 fix put
  into its *table* form (two lists, one updated) — both added.
- **Watch-RMW sweep:** three hits in `capability_handle.rs`; the 2026-07-21 run classified only the
  receiver-relay shape as a false positive, so the two *sender*-borrow compare-then-send publishers
  (`watch_wiring`, `watch_demand`) sat unclassified through four "clean" passes. Not a lost update
  (`next` is computed from KV, not from the borrowed value) — **converted to `send_if_modified`** so
  the sweep is mechanically clean; page sharpened with a classify-by-shape rule.
- **Renamed construct:** `lock-free-and-atomics.md` cited `apply_and_notify`'s `old_ts_if_live`;
  the code has had `old_live` (reset per CAS retry) — fixed.
- **Lock-order labels:** rows 25/28 named the WAL lock's struct `WalInner::inner` (the struct is
  `WalWriter`, the *inner type* is `WalInner`) with a dead line number; row 24 attributed
  `queue_waits_us` to `TupleStore` (it is `StageState`). Labels fixed; no undeclared lock — the
  workspace sweep (46 fields + 2 aliases) reconciles with rows 1–36.
- Constants clean: `WIRE_VERSION` 12 / `PREV` 11 everywhere (the one `v10` mention is the ledger's
  own history); `swim_failure_detector` default `true`; `MAX_KV_WRITE_BYTES` as stated. Named
  gates: 76 snake_case identifiers cited, 4 unresolved — all three test names are *old* names in
  lint logs recording their renames, and `manual_is_multiple_of` is a clippy lint. Features: all
  in `operations.md`. Tags cited by `building-on` exist (`mycelium-reason-v0.6.0` pushed today).

## §2 Staleness — clean
Every durable-knowledge commit since 08-16 has an ingest entry (08-17/18 nightly, 08-18 py pooling,
09-02 review fixes, 09-03 nightly, 09-04 PAIR imports + Run 60); pages touched accordingly.

## §3 Orphans & dead links — 2 findings
- **`wiki.md` (the front door) carried two dead links since 2026-07-24** (`../../operations/…`,
  `../../plans/…` — one `../` too many) through the 07-24, 07-26, and 08-16 passes. Fixed. Ledger.
- Folder-notes all present; no orphans; inbound `examples/README.md#anchor` links all live (the two
  "dead" hits are prose in old lint logs).

## §4 Coverage — 1 finding
- **Enumeration by category:** the tree-derived sweep (44 example units across all crates) found an
  entire category absent from `dev/examples.md`: the tuple-space **operational launchers**
  (`scrape_fleet_node` / `scrape_worker_node`) — documented in the crate's examples README, never on
  the wiki page. Added, plus a line for `examples/presets/` (config presets, not examples — exempt).
- Design docs: every `docs/design/*.md` is cited by a wiki page. UI-example contract: 12 browser
  examples enumerated; 10 compliant (`ui/viz` + console link + concepts), `conway-gpu` and
  `ops_console` the documented exceptions — none unclassified.

Pages touched: `wiki.md`, `dev/examples.md`, `dev/concurrency/lock-order.md`,
`dev/concurrency/lock-free-and-atomics.md`, `guide/building-on-mycelium.md`, `src/lib.rs` (table),
`src/agent/capability_handle.rs` (code), `.github/workflows/ci.yml`, this log, the calibration ledger.
