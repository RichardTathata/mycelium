# 2026-09-04 — v2.4.1 released (security PATCH)

Cut **v2.4.1** (tag `v2.4.1`, commit on `main`). Wire **v12** (PREV 11) unchanged; no `mycelium`
public-API change → rolling upgrade holds. Release gate: CI green on `65e0df6` before tagging.

**Why a patch now:** the v2.4.0 tag is vulnerable twice — wasmtime 46.0.2 (RUSTSEC-2026-0269,
sandbox escape) and the merged-route auth gap (every companion `/gateway/…` surface answered
without a bearer when a gateway token was set; ledger entry, Security 8 at Run 59). Also in: h2
0.4.16 (RUSTSEC-2026-0258), yanked `chacha20` → 0.10.2, `FsStore` erase serialization (row 35),
`GitStore` ref-CAS driver, watch publishers → `send_if_modified`, compliance clippy now in CI.

**Upgrade note (compliance, scoped tokens):** companion routes now need `llm:*` / `wiki:*` /
`board:*` / `tuple:*` (or `*`) — `docs/operations/rbac.md`.

Companions stay on their own version lines: `mycelium-reason` 0.6.0 (tag `mycelium-reason-v0.6.0`,
the PAIR imports), `mycelium-py` 0.2.3. Bumped: root, core, agentfacts, blackboard, tuple-space,
wasm-host, wiki → 2.4.1. Docs: CHANGELOG § [2.4.1] (Security block first), CLAUDE.md, wiki front
door, history ledger, ROADMAP status, building-on tag snippet.
