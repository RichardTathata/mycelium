# wiki-lint — 2026-09-05 (post v2.4.2)

Run after the day's three review-driven fixes (#169 persistence, #172 gateway auth, #171 checkpointer)
and the v2.4.2 cut — 20 commits since the 2026-09-04 lint. Findings by check:

## §0 Calibration ledger
Read first. Two structural lessons applied: the lock-table diff is by `Struct::field` (grouped rows),
and the front-door reserved list must be diffed **per occurrence**. The latter earned its keep (below).

## §1 Doc-vs-code — 4 findings (all doc-side; no code bug)
- **Front-door reserved-prefix list (F1):** `building-on-mycelium.md`'s blockquote form lacked `audit/`
  (the bullet form had it since 2026-09-04) — the third "two lists, one updated" miss; ledger entry +
  mechanical sharpening added. Fixed.
- **`dev/operations.md` heading (F2):** "Public HTTP endpoints … no auth by design" sat over a table
  whose `/consensus/{slot}` row became bearer-gated today (#172). Heading rewritten to state the public
  set exactly (probes · `/bulk/{id}` · A2A descriptor) and point at security §3 / `rbac.md`. Fixed.
- **UI-example contract (F4):** `examples/llm_agent.rs` (Mesh Control, an `include_str! html` hit)
  had in-file run comments without `--features metrics` while the guide's command carries it. Comment
  aligned; the contract page now records the **named** tree-derived classification (10 compliant + 3
  exceptions, 12 hits — `conway` and the `main.rs` hits resolve to `conway`/`conway-gpu`/`ops_console`)
  so the name-grep classifies every hit each pass. Fixed.
- **Lock-order table:** 75 `Mutex<|RwLock<` sites / 3 aliases across all crates reconciled against rows
  1–36 by `Struct::field`; **complete** (today's persistence work added channels, not locks;
  `PrefetchingSource::cache` confirmed at `http_source.rs:43`). **Watch-RMW sweep:** one hit,
  `capability_handle.rs:558` — receiver-borrow relay (`raw_rx.borrow()` → different `tx.send`), the
  documented false-positive shape; zero sender compare-then-send hits (the 09-04 `send_if_modified`
  conversion holds). **Named gates:** all 29 page-cited test names resolve (the three grep misses are
  `.log`-only: a 07-15 rename note and two Python test *files*). **Constants:** wire 12/PREV 11 in
  `framing.rs` = every guide/wiki pin; `swim_failure_detector` default `true`; `MAX_KV_WRITE_BYTES` as
  stated. **KV-namespace table:** 13 `format!("x/` writers outside the 26-row table classified — all
  non-KV or sub-slots (`capauthz/ consistent/ leader/` are consensus *slot* names under `consensus/`;
  `councils/ refs/` git paths; `hot/ k/ perf/ race/ stress/` `store.rs` test-module keys; `llm/` a JSON
  field; `reason/{run}` rides `KvHandle::append` → `log/`; `skill/job-` an audit target). Clean.
  **CI-gate block** (`testing.md`): all ten commands have live `run:` lines incl. the 2026-09-04
  default-feature clippy (`de6912c`). **Endpoint list:** `operations.md` rows match `http.rs` routes
  after F2.

## §2 Staleness — clean
Every merged change since 09-04 has a dated ingest (`pair-imports`, `v2.4.1-release`, three fix logs,
`v2.4.2-release`); `history.md`'s "unreleased" section was converted at the cut; the three
`[Unreleased]` mentions left are historical ("cut from").

## §3 Orphans & dead links — clean
Script resolving every relative link relative to its own file over `docs/wiki/**` + `faq` +
`building-on`: 0 dead, 0 orphans, every folder has its folder-note. Inbound `examples/README.md#…`
anchors: the two non-matching strings (`#anchor`, `#the-suites`) occur only inside `.log` entries
quoting the check itself — not links.

## §4 Coverage — 1 finding
- **`companions.md` did not cite the checkpointer 0.1.1 fix (F3)** — the log existed, the page
  described the Python tier without the async-row-selection change. One-line cite added. Fixed.
- Examples by category (tree-derived: 11 dirs) all represented; `openai_serve`/`ollama_serve` present.
  Plans touched since 09-04 (`mycelium-reason*.md`) are cited from `companions.md` and
  `pattern-coverage.md`. Today's invariants have their page (`runtime-invariants` §Persistence,
  `security.md` §3 addendum) — written at ingest, verified here.

## Pages touched
`docs/guide/building-on-mycelium.md` · `dev/operations.md` · `dev/companions/companions.md` ·
`dev/ui-example-contract.md` · `examples/llm_agent.rs` (doc comment) · `.log/lint-calibration.md` ·
this log. No user decision needed; no new section.
