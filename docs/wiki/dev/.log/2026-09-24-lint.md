## [2026-09-24] lint | doc-vs-code over the day's changes — 7 findings, all fixed

Up: [dev](../dev.md) · scope: `docs/wiki/**` (incl. `.log/`), `docs/guide/`, `docs/operations/`,
`docs/design/`. Focused on what changed 2026-09-23/24 — `mycelium::election` and the three
companions' election sites, P10 and its operator surface, `require_identity_proofs`, the federation
SDK verbs — plus the standing mechanical sweeps.

**Findings, by check.**

§1 doc-vs-code (5):

1. `design/coordination-approaches.md` — three present-tense *"lowest-node-id ring self-election"*
   claims for the tuple space, the blackboard and the wiki. **Fixed**: the ring's negotiated rule,
   with the sentinel's ordering requirement stated (a sentinel that orders curators differently from
   the election leaves two that never converge).
2. `operations/companions.md` — two operator-facing *"lowest-node-id wins"* claims. **Fixed**,
   including *why a half-upgraded fleet can still look concentrated*, which is the question an
   operator will actually arrive with.
3. `design/legible-emergence-taxonomy.md` and `design/network-design-reproducibility-note.md` —
   **my own text from the same PR as the fix**, describing the superseded rule in the present tense.
   **Fixed** to past tense. Worth naming plainly: the drift was introduced by the change that made
   it drift, hours earlier, in the document explaining the change.
4. `wiki/dev/operations.md` — the page that **enumerates** the `/stats` and `/metrics` detector
   gauges listed P1, P6, P2, P3, P4 and not **P10**, whose gauge shipped that day into three other
   docs. **Fixed.** Ledger entry appended: when a check adds an *observable*, diff the observable
   set in code against every page that enumerates it, not only the page describing the new thing.
5. `guide/17-federation.md` — heading *"and what is still to build"* over a body that says row 11 is
   closed. **Fixed.**

§2 staleness (1):

6. `operations/shared-responsibility-matrix.md` — *"TLS on the federation edge itself is not
   shipped"*, after v2.13.0 shipped it. **Fixed**, and the per-partner cap's default (`0` =
   unlimited) added, since that page's job is stating what an adopter must provide themselves.

§1 doc-vs-code, found **after** the pass by a question rather than by the sweep (1):

7b. `plans/v3-contracts-axis.md` §10.2 called `require_identity_proofs` **default-off** — true when
   rev 1.15 was written that morning, false by the afternoon (#368). **Fixed**, and the roots this
   pass sweeps now include `docs/plans/`: the plan of record restates code facts exactly as the
   front-door docs do, and was excluded for no better reason than that the first four roots were
   the habitual ones. Recorded in the ledger — a scope gap found by being asked a question the
   sweep should have answered.

§3 dead links (1):

7. Two `.log/` entries from 2026-09-22 linked `testing/testing.md` **relative to `.log/`** — one
   `../` too many, the
   same shape as the 2026-09-04 ledger entry, in a directory that entry's sharpening never reached.
   **Fixed**; §3 now walks `.log/` too. 0 dead relative links across all four roots afterwards.

**Checks that came back clean** — recorded because a clean check is only useful if it is named:

- **Lock-order table**: the workspace diff since `v2.13.0` adds **no** `Mutex`/`RwLock` field, and
  row 40 (`FederationEdge::in_flight`) matches its field. 40 rows.
- **Named regression gates**: all 14 gates cited in docs added over the last two days resolve to a
  live `fn` (federation verbs, three domains, CN provenance, the edge budget, the open-gateway
  regression, the three identity-proof pins, the P10 set, the election set).
- **Cited constants**: `ROLE_CONCENTRATION_TRIP_PERCENT = 60` matches the taxonomy's *"≥ 60%"* and
  the Prometheus alert's `>= 60`.
- **Endpoint shapes**: every `GET|POST /gateway/federation/…` named in the runbooks, the guide, the
  wiki and both SDK READMEs exists as a literal route in `src/agent/http.rs`.

---

## Second pass, same day: the standing sweeps the focused run scoped out

The morning's pass was scoped to what changed 2026-09-23/24. This one ran the whole checklist.

**§1 (3 more findings), all on the front door — the highest-stakes page, because an integrator
*acts* on it:**

8. **The install snippet pointed at `github.com/RichardEko/mycelium`** — the repository moved to
   `RichardTathata` around 2026-07-31. It resolves today only on GitHub's rename redirect, and an
   abandoned account name someone else can later claim is a **supply-chain hazard**, not a typo.
   Fixed here and in two guide-14 permalinks. **The 5 references in `docs/publications/` are left
   alone and reported** — published papers and decks are `/publication-lint`'s scope, and a
   published artefact's URL is a historical fact, not drift.
9. **The core pin read `tag = "v2.4.4"`** — nine releases stale. The pin check was added on
   2026-09-06 and no pass had run since.
10. **Both reserved-prefix lists were missing all four v3 prefixes** (`knowledge/`, `mandate/`,
    `rights/`, `cn/`); the blockquote also lacked seven older companion ones. This is the **fourth**
    occurrence of the "two lists, one updated" family, and the plan's §7 had *required* both lists to
    be updated at each item's PR 1.

    Two prior sharpenings said *"diff every occurrence, mechanically"* and it drifted anyway,
    **because nothing ran the diff**. So this one is not another point patch: the diff is now a
    **gate** in `scripts/check-kv-namespaces.sh` (already in `make check`) — each `src/lib.rs`
    prefix must appear **twice** in the front door, once per list. Planted a single removed prefix:
    it names the prefix and **exits 1**.

**Clean on this pass:** `kv_ns` prefixes all have ownership rows · `WIRE_VERSION` 12 / `PREV` 11
matches every guide claim · both companion tag pins (`guardrails-v1.0.0`, `reason-v0.6.2`) are the
newest on their lines.

**Pages touched:** `design/coordination-approaches.md`, `design/legible-emergence-taxonomy.md`,
`design/network-design-reproducibility-note.md`, `operations/companions.md`,
`operations/shared-responsibility-matrix.md`, `guide/17-federation.md`, `wiki/dev/operations.md`,
two `.log/` entries, `guide/building-on-mycelium.md`, `guide/14-patterns-and-pitfalls.md`,
`plans/v3-contracts-axis.md`, `scripts/check-kv-namespaces.sh` (the new gate), and
`lint-calibration.md` (five sharpenings).

**Nothing needs a user decision.** No finding turned out to be a code bug: in all seven the code was
right and the page was behind.
