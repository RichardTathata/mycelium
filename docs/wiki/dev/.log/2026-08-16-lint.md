# wiki-lint — 2026-08-16 (post council-substrate arc)

Run after the substrate arc (GitMirror · GitStore phases 1–5 · hardening P6.1–P6.6 · the ingest
edge; ~36 commits since the 2026-07-26 lint). Findings by check:

## §1 Doc-vs-code — 1 CODE finding (fixed in code, not papered in the wiki)
- **The lock-order table's flat invariant was violated by the new code**, not just missing rows:
  `GitStore::write_with` held `write_lock` while its `load_at` acquired `cat_file` — the
  workspace's first nested acquisition (benign: strict one-way order, but the invariant is "one
  lock per function, no ordering discipline needed"). **Fixed the code**: the write path's rare
  current-state read uses a spawn-based `load_at_spawned` (one extra spawn amid the ~7 a commit
  already makes) while the corpus-scale read paths keep the P6.2 `cat_file` child. Flatness holds
  workspace-wide again; all 33 git-store/curator tests green.
- **Three undeclared locks** → rows 32–34 added (`GitStore::write_lock` — held across local git
  subprocess I/O by design, documented; `GitStore::cat_file` — µs pipe round-trips;
  `GitMirror::busy` — try-lock-and-skip, never blocks).
- Named gates: every test cited by the wiki exists (headline six verified by grep). Watch-RMW
  sweep clean; wire constants untouched by the arc.

## §2 Staleness — 1 finding (fixed)
- `companions/wiki.md`'s GitStore section described **Phase 1 only** — zero mentions of
  `for_group`, `validate_cmd`, ingest, `write_pages`, `refresh`/`publish`, `PageFormat`, or the
  gateway ingest edge, all shipped since. Added a compact Phases 2–6 block with the two recorded
  measurements and updated the Gates line (work_distribution + the ingest edge).

## §3 Orphans/links — clean (new links verified at write time; the two new cross-links resolve).

## §4 Coverage — 1 finding (fixed)
- `docs/plans/council-substrate-hardening.md` (the authoritative Phase-6 record with the
  measurements) was cited only from `.log` entries, not from any wiki **page** — the sharpened §4
  rule ("the wiki must cite it, not merely know of it"). Now linked from the companion page's
  Phases 2–6 block.

No calibration-ledger entry: all drift is post-2026-07-26 work caught by the first lint after it —
normal operation, no prior-lint miss. The §1 code finding is the check's design working as
intended ("if the code is wrong, fix the code, not the page").
