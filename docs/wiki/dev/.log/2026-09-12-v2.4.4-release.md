## [2026-09-12] ingest | v2.4.4 released (durability PATCH: snapshot rename fsynced before WAL truncation)

- Cut **v2.4.4** at the consumer's request (NovusLens re-pin: their durable canon rides the snapshot/WAL path). Delta
  over v2.4.3: the directory fsync after the snapshot rename (`237352a`), `/consensus/{*slot}` tail capture,
  `mycelium-reason` 0.6.1/0.6.2, honest `set_with_min_acks` docs, stub-server backlog. Wire v12 / PREV 11 unchanged;
  `src/lib.rs` untouched (reserved table unchanged); `mycelium-py` stays 0.2.4 (docstring-only change since).
  Bumped: root, core, agentfacts, blackboard, tuple-space, wasm-host, wiki → 2.4.4. Docs: CHANGELOG § [2.4.4],
  CLAUDE.md, wiki front door, history ledger, ROADMAP epoch note, building-on tag snippet.
